// use std::collections::HashMap;

// static USER_WALLETS: std::sync::LazyLock<HashMap<String, String>> = std::sync::LazyLock::new(|| HashMap::new());
// pub fn store_wallet(user: &str, wallet: &str) {
//     unsafe {
//         USER_WALLETS.insert(user.to_string(), wallet.to_string());
//     }
// }